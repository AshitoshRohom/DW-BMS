<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- SALES USER: SEE ONLY THEIR OWN SALES ORDERS -->
    <record id="sale_order_rule_sales_own" model="ir.rule">
        <field name="name">Sales User: Own Sales Orders Only</field>
        <field name="model_id" ref="sale.model_sale_order"/>

        <!-- IMPORTANT: use salesperson -->
        <field name="domain_force">
            [('user_id', '=', user.id)]
        </field>

        <field name="groups"
               eval="[(4, ref('sales_team.group_sale_salesman'))]"/>

        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- ADMIN: SEE ALL SALES -->
    <record id="sale_order_rule_admin_all" model="ir.rule">
        <field name="name">Admin: All Sales Orders</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="domain_force">[(1,'=',1)]</field>
        <field name="groups" eval="[(4, ref('base.group_system'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

</odoo>
